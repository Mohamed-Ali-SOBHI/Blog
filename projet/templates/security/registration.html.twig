{% extends 'base.html.twig'%}

{% block body %}
    <h1> Inscription </h1>

    {{ form_start( form ) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password)}}
    {{ form_row(form.confirme_password)}}
    <button type='submit' class='btn btn-success'> Inscription </button>
    {{ form_end(form)}}


{% endblock %}